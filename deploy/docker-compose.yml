version: '3.7'

services:
  letsencrypt:
    image: adferrand/dnsrobocert:3.27.0@sha256:88ebafa8895a0a4bad1ca415272fdcd86b53e90cb7ed3298c2508f3f217b35c7
    container_name: letsencrypt-dns
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./letsencrypt:/etc/letsencrypt
      - ./le-config.yml:/etc/dnsrobocert/config.yml
    restart: always
  sish:
    image: ghcr.io/glueops/sish:latest@sha256:8cbd397601616d8a0b767fa20f670a998f1d21dd8385a5277fa9ffa84c699daa
    container_name: sish
    depends_on: 
      - letsencrypt
    volumes:
      - ./letsencrypt:/etc/letsencrypt
      - ./pubkeys:/pubkeys
      - ./keys:/keys
      - ./ssl:/ssl
    command: |
      --ssh-address=:22
      --http-address=:80
      --https-address=:443
      --https=true
      --https-certificate-directory=/ssl
      --authentication-keys-directory=/pubkeys
      --private-keys-directory=/keys
      --bind-random-ports=false
      --bind-random-subdomains=false
      --domain=ssi.sh
    network_mode: host
    restart: always
